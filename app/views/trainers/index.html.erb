<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="container">

<p id="notice"><%= notice %></p>

<h1>Trainers</h1>
  <div class="row">
    <div class="col-4"
      <ul class ="list-group">
        <% @trainers.each do |trainer| %>
          <li class="list-group-item">Trainer:<%= link_to trainer.name, trainer_path(trainer.id) %></li>
        <% end %>
      </ul>
    </div>
  </div>
